<%= render 'shared/sub_catalogues', sub_catalogues: @sub_catalogues %>

<div class="col-9 content">

	<%= render partial:'shared/poster', locals: { img_url: 'poster.png' } %>

	<div class="row col-12 soluongsp">
		<p><%= catalogues.first.products.length %> sản phẩm có sẵn</p>
	</div>
	<div class="row col-12 products">
			
			<%= render @products %>

		<%= will_paginate @products %>

	</div>				
</div>